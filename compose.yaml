services:
  searxng:
    image: searx/searx:latest
    container_name: searxng
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./config/:/etc/searxng/
      - ./data/:/var/cache/searxng/
